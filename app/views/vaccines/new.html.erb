<% content_for :title, "Add #{@pet.name}'s vaccine" %>
<section class="content-card"><p class="eyebrow"><%= @pet.name %>'s health</p><h1>Add vaccine</h1><%= render "form", pet: @pet, vaccine: @vaccine %></section>
